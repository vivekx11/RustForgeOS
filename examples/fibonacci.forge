// Fibonacci sequence in ForgeScript

fn fibonacci(n: int) -> int {
    if n <= 1 {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() -> int {
    let n: int = 10;
    let result: int = fibonacci(n);
    print(result);
    return 0;
}
