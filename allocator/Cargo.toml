[package]
name = "allocator"
version.workspace = true
edition.workspace = true

[dependencies]
spin = { workspace = true }
x86_64 = "0.14"
linked_list_allocator = "0.10"

[features]
default = []
buddy = []  # Enable buddy allocator instead of linked list

[lib]
